{
  "name": "firestore-tests",
  "description": "Firestore security rules tesztek (emulator ellen) + redeem-core integracios tesztek",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "firebase-admin": "^13.0.0"
  }
}
